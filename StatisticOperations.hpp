#pragma once

#include <vector>
#include <memory>
#include <algorithm>

long double getMedian(std::unique_ptr<std::vector<long double>>);
long double getAverage(std::unique_ptr<std::vector<long double>>);